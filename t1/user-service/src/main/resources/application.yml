server:
  port: 8081 # 用户服务运行端口
spring:
  application:
    name: user-service # 注册到 Eureka Server 的服务名称
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # Eureka Server 的地址